Канал -- это механизм связи между горутинами.
Он позволяет передавать значения определённого типа.

```go
ch := make(chan int) // канал для int
```

=== Основные операции

```go
ch <- x   // отправка
x = <-ch  // получение
<-ch      // получение (игнорируем значение)
```

=== Небуферизованный канал

Отправка блокируется, пока кто-то не прочитает.
Получение блокируется, пока кто-то не отправит.

```go
ch := make(chan int)
go func() { ch <- 42 }()
x := <-ch // 42
```

=== Буферизованный канал

```go
ch := make(chan string, 2)
ch <- "hello"
ch <- "world"
fmt.Println(<-ch) // hello
fmt.Println(<-ch) // world
```

Отправка блокируется только когда буфер заполнен.
Получение блокируется когда буфер пуст.


=== Закрытие канала

```go
close(ch)
```

- После закрытия: чтение возвращает значение `+ ok == false`
- Запись в закрытый канал вызывает панику

=== Однонаправленные каналы

```go
var ch1 chan<- int // только отправка
var ch2 <-chan int // только получение
```

=== Конвейер (pipeline)

```go
func main() {
    naturals := make(chan int)
    squares := make(chan int)

    go func() {
        for x := 0; x < 10; x++ {
            naturals <- x
        }
        close(naturals)
    }()

    go func() {
        for x := range naturals {
            squares <- x * x
        }
        close(squares)
    }()

    for x := range squares {
        fmt.Println(x)
    }
}
```

=== Итог

- Каналы -- главный способ общения между горутинами.
- Небуферизованные -- синхронные.
- Буферизованные -- асинхронные (до заполнения буфера).
- Закрытие канала -- сигнал, что данных больше не будет.
