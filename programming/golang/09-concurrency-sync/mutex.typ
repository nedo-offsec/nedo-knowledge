=== Проблема

Гонка данных возникает, когда несколько горутин одновременно обращаются к
одной переменной, и хотя бы одна из них пишет.

=== Решение: `sync.Mutex`

Мьютекс обеспечивает взаимное исключение -- доступ к критическому
разделу получает только одна горутина за раз.

```go
import "sync"

var mu sync.Mutex
var balance int

func Deposit(amount int) {
    mu.Lock()          // захват блокировки
    balance += amount
    mu.Unlock()        // освобождение блокировки
}

func Balance() int {
    mu.Lock()
    defer mu.Unlock()  // освобождение через defer
    return balance
}
```

=== Критический раздел (critical section)

Область между `Lock()` и `Unlock()` -- это критический раздел.
В нём горутина может безопасно читать и изменять защищаемые переменные.

=== defer для Unlock()

Используй `defer mu.Unlock()` сразу после `Lock()` -- это
гарантирует освобождение блокировки даже при панике или раннем возврате.

=== Мьютекс не реентерабельный

Мьютекс нельзя заблокировать повторно в той же горутине. Это приведёт к взаимоблокировке (`deadlock`).

```go
func Withdraw(amount int) bool {
    mu.Lock()
    defer mu.Unlock()
    Deposit(-amount) // deadlock! Deposit пытается захватить тот же мьютекс
    // ...
}
```

Решение: разделить логику на внутреннюю функцию (без блокировки)
и внешнюю (с блокировкой).

=== Паттерн: монитор

Набор экспортируемых функций, инкапсулирующих переменные и выполняющих
блокировку/освобождение.

=== Ключевая мысль

Мьютекс -- это простой и надёжный способ защитить общие данные.
Главное правило: всегда освобождай блокировку через `defer`.

