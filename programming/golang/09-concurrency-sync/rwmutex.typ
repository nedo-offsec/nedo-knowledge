=== Проблема

Когда много горутин читают данные, а пишут редко, обычный `sync.Mutex`
блокирует всех, даже читателей, хотя они не мешают друг другу.

=== Решение sync.RWMutex

`RWMutex` позволяет:
- *Несколько читателей* одновременно (`RLock`)
- *Только одного писателя* за раз (`Lock`)

```go
var mu sync.RWMutex
var balance int

func Balance() int {
    mu.RLock()         // блокировка для чтения
    defer mu.RUnlock()
    return balance
}

func Deposit(amount int) {
    mu.Lock()          // исключительная блокировка
    defer mu.Unlock()
    balance += amount
}
```

=== Правила

- `RLock()` / `RUnlock()` -- для читателей.
- `Lock()` / `Unlock()` -- для писателей.

Читатели не блокируют друг друга.
Писатель блокирует всех (и читателей, и других писателей).

=== Когда использовать

`RWMutex` имеет смысл, если:

+ Чтений гораздо больше, чем записей.
+ Блокировка -- узкое место (горутины часто ждут).

`RWMutex` медленнее обычного `sync.Mutex` из-за более сложной логики.
Не используй его без необходимости.

=== Важно

`RLock()` можно использовать только когда
критический раздел точно не пишет в общие данные.
Если сомневаешься -- используй обычный `Lock().`

=== Ключевая мысль

`RWMutex` -- это оптимизация для сценариев с
большим количеством чтений и редкими записями.
Для остальных случаев используй обычный `sync.Mutex`.
