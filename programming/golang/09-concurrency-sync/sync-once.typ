=== Проблема

Нужно выполнить инициализацию только один раз, но функция может быть вызвана параллельно из многих горутин.

Простая проверка `if icons == nil` не работает из-за гонок данных и переупорядочивания памяти.

=== Решение: `sync.Once`

```go
var loadIconsOnce sync.Once
var icons map[string]image.Image

func Icon(name string) image.Image {
    loadIconsOnce.Do(loadIcons)
    return icons[name]
}
```

=== Как работает Do

- Первый вызов `Do(f)` выполняет `f()`.
- Последующие вызовы `Do(f)` ничего не делают.
- Do гарантирует, что `f()` выполнится только один раз, даже при параллельных вызовах.
- Do блокирует вызовы до завершения `f()`.

=== Почему лучше, чем мьютекс

#table(
  columns: (auto, auto),
  [*Подход*], [*Проблема*],
  [`sync.Mutex`], [Блокирует всех даже после инициализации],
  [`sync.RWMutex`], [Сложный код, нужно перепроверять `nil`],
  [`sync.Once`], [Простой, безопасный, эффективный],
)

=== Итог

`sync.Once` -- идеальный инструмент для ленивой инициализации.
Гарантирует, что функция выполнится ровно один раз, даже при
параллельных вызовах, и все горутины увидят результат.
