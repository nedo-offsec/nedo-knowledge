=== Что такое состояние гонки (race condition)

Состояние гонки -- ситуация, когда программа даёт неправильный результат при некотором чередовании операций нескольких горутин.

=== Гонка данных (data race)

Гонка данных возникает, когда две горутины одновременно обращаются к одной
переменной, и хотя бы одно обращение -- запись.

=== Пример: банковский счёт

```go
var balance int

func Deposit(amount int) {
    balance = balance + amount // ЧТЕНИЕ + ЗАПИСЬ
}

func Balance() int {
    return balance
}
```

Проблема: `balance = balance + amount` -- это две операции:

- Чтение balance
- Запись новой суммы
- Если две горутины вызовут `Deposit` одновременно, может произойти гонка.


=== Как это выглядит

Горутина А: читает `balance` (0) -> `balance + 200 = 200`
Горутина Б: читает `balance` (0) -> `balance + 100 = 100`
Горутина А: записывает 200
Горутина Б: записывает 100
Результат: 100, хотя должно быть 300.

=== Три способа избежать гонки

- Не записывать переменную -- только чтение.
- Ограничить переменную одной горутиной (например, через каналы).
- Взаимное исключение -- допускать доступ только одной горутине за раз (Mutex).

=== Пример: ограничение через каналы

```go
var deposits = make(chan int)
var balances = make(chan int)

func Deposit(amount int) { deposits <- amount }
func Balance() int       { return <-balances }

func teller() {
    var balance int
    for {
        select {
        case amount := <-deposits:
            balance += amount
        case balances <- balance:
        }
    }
}

func init() {
    go teller()
}
```

=== Ключевая мысль

Гонка данных -- это всегда ошибка. Нет "доброкачественных" гонок.
Избегай их тремя способами: не пиши, ограничь горутину, или используй мьютексы.

