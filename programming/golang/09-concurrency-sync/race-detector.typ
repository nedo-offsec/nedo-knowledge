=== Что это

Детектор гонки -- инструмент в Go, который находит гонки данных во время
выполнения программы.

=== Как использовать

Добавь флаг `-race` к командам `go build`, `go run` или `go test`:

```bash
go run -race main.go
go build -race main.go
go test -race ./...
```

=== Как работает

+ Компилятор создаёт специальную версию программы.
+ Программа записывает все обращения к переменным и события синхронизации
  (каналы, мьютексы, горутины).
+ Детектор ищет одновременный доступ к одной переменной без синхронизации.
+ При обнаружении выводит отчёт с указанием места и стеков вызовов.

=== Пример отчёта

```bash
WARNING: DATA RACE
Write at 0x00c0000a00c0 by goroutine 7:
    main.Deposit()
    bank.go:10 +0x5a

Previous read at 0x00c0000a00c0 by goroutine 8:
    main.Balance()
    bank.go:15 +0x4a
```

=== Ограничения

- Детектор находит только те гонки, которые реально произошли во время выполнения.
- Не может найти гонки, которые не были запущены тестами.
- Программа с `-race` работает медленнее и требует больше памяти.

=== Ключевая мысль

Детектор гонки -- это обязательный инструмент при разработке параллельных программ.
Используй его в тестах и при отладке.
